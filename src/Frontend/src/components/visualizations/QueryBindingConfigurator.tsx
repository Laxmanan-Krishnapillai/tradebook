import type { ChartWidgetConfig, VisualEncodingSpec } from '../../types/visualizations';
export function QueryBindingConfigurator({ widget, availableColumns, onChange }: { widget: ChartWidgetConfig; availableColumns: string[]; onChange: (encodings: VisualEncodingSpec) => void }) { return <fieldset><legend>Visual encoding</legend><label>X axis<select value={widget.visualEncodings.xAxis} onChange={(event) => onChange({ ...widget.visualEncodings, xAxis: event.target.value })}>{availableColumns.map((column) => <option key={column}>{column}</option>)}</select></label></fieldset>; }
