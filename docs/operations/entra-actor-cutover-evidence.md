# Entra actor cutover evidence

This release artifact is completed and signed outside source control because it contains identity mappings. Record UTC query output counts for `users`, mutable `workspace_dashboards` owners, and distinct historical `audit_log` actors. Attach the tenant-owner-approved local UUID → Entra `oid` mapping or signed zero-row evidence. Record the reviewed transaction that migrates mutable ownership and a before/after digest proving `audit_log.actor_id` was not changed. Any unmapped active owner is a release blocker.
