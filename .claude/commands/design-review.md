---
description: Review a running Tradebook UI across canonical viewports.
argument-hint: --url <running-ui-url>
---

Delegate to the `design-review` agent with `$ARGUMENTS`. Require the structured score,
selector evidence, prioritized fixes, and Hallmark pass defined in
`docs/design/design-principles.md`. Attach the resulting report and screenshots to the
pull request; do not treat it as a substitute for Visual QA checks.
